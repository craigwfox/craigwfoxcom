---
layout: layouts/base.njk
title: Blog
ogType: article
description: Ramblings on front-end web development
---

<section id="blog" aria-labelledby="title-blog" class="wrapper section">
  <h1 id="title-blog">Ramblings of me</h1>

  <div class="articles-wrapper articles-wrapper--wide">
    {%- for post in collections.post | reverse -%}
    <article class="article">
      <a href={{ post.data.permalink }}>
        <time>{{ post.date | postDate }}</time>
        <h3>
            {{ post.data.title }}
        </h3>
        <p>{{ post.data.description }}</p>
      </a>
    </article>
    {%- endfor -%}
  </div>
</section>